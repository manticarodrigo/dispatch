query PlaceByDevice(
  $agentId: ID!
  $deviceId: ID!
  $placeId: ID!
  $filters: TaskFilters
) {
  place(agentId: $agentId, deviceId: $deviceId, placeId: $placeId) {
    id
    name
    phone
    email
    description
    lat
    lng
    tasks(agentId: $agentId, deviceId: $deviceId, filters: $filters) {
      id
      startAt
      stops {
        id
        place {
          id
          name
          description
          lat
          lng
        }
        arrivedAt
      }
      route
    }
  }
}
