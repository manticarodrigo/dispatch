query Task($taskId: ID!) {
  task(taskId: $taskId) {
    id
    startAt
    seat {
      id
      name
      location {
        id
        position
        createdAt
      }
    }
    stops {
      id
      place {
        id
        name
        description
        lat
        lng
      }
      order
      note
      arrivedAt
    }
    route
  }
}
