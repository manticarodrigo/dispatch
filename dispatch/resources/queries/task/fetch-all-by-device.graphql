query TasksByDevice($seatId: ID!, $deviceId: ID!, $filters: TaskFilters) {
  tasks(seatId: $seatId, deviceId: $deviceId, filters: $filters) {
    id
    seat {
      id
      name
    }
    startAt
    stops {
      id
      place {
        id
        name
        description
        lat
        lng
      }
    }
    route
  }
}
