query Tasks($filters: TaskFilters) {
  tasks(filters: $filters) {
    id
    agent {
      id
      name
    }
    startAt
    stops {
      id
      place {
        id
        name
        description
        lat
        lng
      }
    }
    route
  }
}
