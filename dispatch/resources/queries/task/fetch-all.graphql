query Tasks($filters: TaskFilters) {
  tasks(filters: $filters) {
    id
    seat {
      id
      name
    }
    startAt
    stops {
      id
      place {
        id
        name
        description
        lat
        lng
      }
    }
    route
  }
}
