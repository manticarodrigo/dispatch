query TaskByDevice($seatId: ID!, $deviceId: ID!, $taskId: ID!) {
  task(seatId: $seatId, deviceId: $deviceId, taskId: $taskId) {
    id
    startAt
    seat {
      id
      name
      location {
        id
        position
        createdAt
      }
    }
    waypoints {
      id
      place {
        id
        name
        description
        lat
        lng
      }
      order
      note
      arrivedAt
    }
    route
  }
}
