query Seat($seatId: ID!, $filters: TaskFilters) {
  seat(seatId: $seatId, filters: $filters) {
    id
    name
    device {
      id
      info
    }
    tasks {
      id
      startAt
      waypoints {
        id
        place {
          id
          name
          description
          lat
          lng
        }
        arrivedAt
      }
      route
    }
    location {
      id
      lat
      lng
      createdAt
    }
  }
}
