query Seat($seatId: ID!, $filters: TaskFilters) {
  seat(seatId: $seatId, filters: $filters) {
    id
    name
    device {
      id
      info
    }
    location {
      id
      position
      createdAt
    }
    tasks {
      id
      startAt
      stops {
        id
        place {
          id
          name
          description
          lat
          lng
        }
        arrivedAt
      }
      route
    }
  }
}
