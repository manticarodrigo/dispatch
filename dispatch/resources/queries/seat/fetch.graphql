query Seat($id: ID!) {
  seat(id: $id) {
    id
    name
    routes {
      id
      startAt
      stops {
        id
        address {
          id
          name
          description
          lat
          lng
        }
      }
    }
    location {
      id
      lat
      lng
      createdAt
    }
  }
}
