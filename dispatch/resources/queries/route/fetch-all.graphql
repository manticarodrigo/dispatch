query Routes($filters: RouteFilters, $status: String) {
  routes(filters: $filters, status: $status) {
    id
    seat {
      id
      name
    }
    startAt
    stops {
      id
      address {
        id
        name
        description
        lat
        lng
      }
    }
  }
}
