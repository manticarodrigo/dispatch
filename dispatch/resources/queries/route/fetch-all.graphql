query Routes($filters: RouteFilters) {
  routes(filters: $filters) {
    id
    seat {
      id
      name
    }
    startAt
    stops {
      id
      address {
        id
        name
        description
        lat
        lng
      }
    }
  }
}
