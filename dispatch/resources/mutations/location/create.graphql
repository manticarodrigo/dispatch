mutation CreateLocation(
  $seatId: ID!
  $deviceId: ID!
  $position: LocationPosition!
  $createdAt: String
) {
  createLocation(
    seatId: $seatId
    deviceId: $deviceId
    position: $position
    createdAt: $createdAt
  ) {
    id
    position
    createdAt
  }
}
