mutation CreateLocation(
  $agentId: ID!
  $deviceId: ID!
  $position: LocationPosition!
  $createdAt: Date
) {
  createLocation(
    agentId: $agentId
    deviceId: $deviceId
    position: $position
    createdAt: $createdAt
  ) {
    id
    position
    createdAt
  }
}
