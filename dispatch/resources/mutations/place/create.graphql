mutation CreatePlace(
  $agentId: ID
  $deviceId: ID
  $name: String!
  $phone: String
  $email: String
  $description: String!
  $lat: Float!
  $lng: Float!
) {
  createPlace(
    agentId: $agentId
    deviceId: $deviceId
    name: $name
    phone: $phone
    email: $email
    description: $description
    lat: $lat
    lng: $lng
  ) {
    id
    name
    phone
    email
    description
    lat
    lng
  }
}
