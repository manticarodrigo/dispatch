mutation CreateRoute(
  $seatId: ID!
  $startAt: String!
  $addressIds: [ID]!
  $route: JSON!
) {
  createRoute(
    seatId: $seatId
    startAt: $startAt
    addressIds: $addressIds
    route: $route
  )
}
