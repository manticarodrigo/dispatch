(ns app.config)

(def debug?
  ^boolean goog.DEBUG)

(goog-define REGION "")
(goog-define LANGUAGE "")
(goog-define API_URL "")
(goog-define SECURE_COOKIE "")
(goog-define GOOGLE_MAPS_API_KEY "")
