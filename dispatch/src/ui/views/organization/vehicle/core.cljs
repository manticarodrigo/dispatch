(ns ui.views.organization.vehicle.core
  (:require [ui.views.organization.vehicle.list :as list]
            [ui.views.organization.vehicle.create :as create]))

(def list-view list/view)
(def create-view create/view)
