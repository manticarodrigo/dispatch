mutation CreateShipments($shipments: [ShipmentInput]!) {
  createShipments(shipments: $shipments) {
    id
    shipments {
      id
      weight
      volume
      windows {
        id
        startAt
        endAt
      }
    }
  }
}
